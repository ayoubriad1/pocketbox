# Python dependencies (pip).
# NOTE: fpocket is NOT a pip package — install it separately (bioconda or
# source). The conda route in environment.yml installs everything incl. fpocket.
streamlit>=1.30
py3Dmol>=2.0
stmol>=0.0.9
rdkit>=2023.9
numpy>=1.24
